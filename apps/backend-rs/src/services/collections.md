# collections.rs

Collection service. Enriches collections with note counts.

## Exports
- `get_with_count()` — fetches a single collection with its note count
- `list_with_counts()` — fetches all collections with note counts
