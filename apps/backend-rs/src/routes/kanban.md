# kanban.rs

Kanban route definitions covering boards, columns, tickets, labels,
activities, attachments, and SSE.

## Exports
- `/api/kanban/*` — routes for boards, columns, tickets, labels, activities, attachments, time logs, sprints, and SSE board stream
