# activity.rs

Activity model for board and ticket audit logs. Stores metadata as JSON.

## Exports
- `ActivityRow` — database row struct (metadata stored as JSON string)
- `Activity` — API response struct with parsed metadata JSON
