# calendar_event.rs

Calendar event model with all-day event support.

## Exports
- `CalendarEventRow` — database row struct (all_day stored as integer)
- `CalendarEvent` — API response struct (all_day as bool)
