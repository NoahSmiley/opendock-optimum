# boards.rs

Board CRUD handlers.

## Exports
- `list_boards()` — list all boards for the current user
- `create_board()` — create a new board
- `get_board()` — get a full board snapshot by id
- `update_board()` — update board details
