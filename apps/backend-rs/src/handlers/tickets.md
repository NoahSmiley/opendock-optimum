# tickets.rs

Ticket CRUD, reordering, and comment handlers.

## Exports
- Ticket CRUD handlers (create, get, update, delete)
- `reorder()` — move/reorder a ticket within or across columns
- `add_comment()` — add a comment to a ticket
- `delete_comment()` — remove a comment from a ticket
