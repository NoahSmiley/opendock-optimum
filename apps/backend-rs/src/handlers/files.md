# files.rs

File upload, update, delete handlers and file folder management.

## Exports
- `upload()` — multipart file upload handler
- `update()` — update file metadata
- `delete()` — delete a file (disk + DB)
- File folder CRUD handlers
