# time_logs.rs

Time log handlers for tracking time on tickets.

## Exports
- `start()` — start a time log
- `stop()` — stop the active time log
- `list()` — list time logs for a ticket
- `delete()` — delete a time log
