# activities.rs

Database queries for activity audit logs.

## Exports
- `log()` — insert an activity record
- `list_by_board()` — fetch activities for a board
- `list_by_ticket()` — fetch activities for a ticket
