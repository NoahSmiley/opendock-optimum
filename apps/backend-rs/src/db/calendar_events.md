# calendar_events.rs

Database queries for calendar events with date range filtering.

## Exports
- Calendar event CRUD functions (create, get, update, delete)
- `list_by_range()` — fetch events within a date range
