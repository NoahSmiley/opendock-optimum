# columns.rs

Database queries for board columns, including a max-order helper for
ordering new columns.

## Exports
- Column CRUD functions (create, list by board, update, delete)
- `max_order()` — returns the highest column order for a given board
