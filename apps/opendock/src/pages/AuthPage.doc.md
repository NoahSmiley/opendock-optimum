# AuthPage

Auth page that toggles between login and register forms.

## State
- `mode` — "login" or "register"

## Used by
- `App.tsx` as the unauthenticated route

## Dependencies
- `LoginForm`, `RegisterForm`
