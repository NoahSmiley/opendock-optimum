# ticketHelpers

Utility functions for ticket display logic.

## Exports
- `getDueDateStatus(dueDate)` — Returns "overdue", "due-soon", or "upcoming" based on date proximity
- `DueDateStatus` type

## Used by
- `TicketCard.tsx`
