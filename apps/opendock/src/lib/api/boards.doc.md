# boards.ts

Board API functions — CRUD for boards, columns, tickets, labels, sprints, comments.

## Functions
- `fetchBoards()`, `fetchBoard(id)`, `createBoard()`, `updateBoard()`
- `createColumn()`, `updateColumn()`, `deleteColumn()`
- `createTicket()`, `updateTicket()`, `deleteTicket()`, `reorderTicket()`
- `addComment()`, `deleteComment()`
- `createLabel()`, `createSprint()`

## Used by
- `stores/boards/actions.ts`
