# LabelSettings

Settings panel for listing and creating board labels with preset color selection.

## Props
- `board` — The Board whose labels are managed

## Used by
- `BoardSettingsModal.tsx`

## Dependencies
- `@/stores/boards/actions` (createLabel)
- `lucide-react` (Plus, Trash2)
- `@/stores/boards/types` (Board)
