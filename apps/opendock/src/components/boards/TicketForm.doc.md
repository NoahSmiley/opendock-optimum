# TicketForm

Form for creating a new ticket with title, description, and priority.

## Props
- `columnId` — target column for the new ticket
- `onSubmit` — called with form data on submit
- `onCancel` — close the form

## Used by
- `BoardsPage` (shown as modal/inline when adding a ticket)
