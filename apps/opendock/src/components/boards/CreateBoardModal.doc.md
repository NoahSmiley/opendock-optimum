# CreateBoardModal

Modal form for creating a new board with a name and optional description.

## Props
- `onSubmit` — Async callback receiving `(name, description?)` to create the board
- `onClose` — Callback to close the modal

## Used by
- `BoardsPage.tsx`

## Dependencies
- `lucide-react` (X)
