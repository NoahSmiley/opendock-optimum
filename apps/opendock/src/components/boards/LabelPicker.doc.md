# LabelPicker

Toggle-able label chips for selecting labels on a ticket.

## Props
- `labels` — available labels
- `selected` — currently selected label IDs
- `onChange` — callback with updated label IDs

## Used by
- `TicketForm`, `FilterBar`
