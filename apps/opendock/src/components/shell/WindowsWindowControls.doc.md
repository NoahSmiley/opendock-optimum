# WindowsWindowControls

Renders Windows-style window controls (minimize, maximize, close).

## Props
None — uses Tauri window API directly.

## Used by
- `Titlebar.tsx` (rendered only on Windows in Tauri)
