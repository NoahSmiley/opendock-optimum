# AppLayout

Layout wrapper that renders the sidebar alongside the main content outlet.

## Structure
- Sidebar (left)
- Content area with `<Outlet />` for route pages (right)

## Used by
- `App.tsx` as the layout route for authenticated pages

## Dependencies
- `Sidebar.tsx`
