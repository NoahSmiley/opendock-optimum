# Sidebar

Navigation sidebar with links to Dashboard, Boards, Notes, Calendar, Files.

## Features
- Uses `NavLink` for active state highlighting
- Collapsible via toggle button (persisted in UI store)
- Icons from lucide-react

## Used by
- `AppLayout.tsx`

## Dependencies
- `stores/ui/store.ts`
