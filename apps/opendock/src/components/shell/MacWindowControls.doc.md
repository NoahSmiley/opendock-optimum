# MacWindowControls

Renders macOS-style traffic light window controls (close, minimize, maximize).

## Props
None — uses Tauri window API directly.

## Used by
- `Titlebar.tsx` (rendered only on macOS in Tauri)
