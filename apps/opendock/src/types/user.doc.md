# user.ts

User-related type definitions shared across the app.

## Types
- `User` — authenticated user profile
- `SessionResponse` — response from GET /api/auth/session
- `AuthResponse` — response from login/register endpoints

## Used by
- `stores/auth/types.ts`, `lib/api/auth.ts`
