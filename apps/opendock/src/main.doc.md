# main.tsx

Application entry point. Mounts the React app to the DOM and imports global stylesheets.

## Responsibilities
- Import global CSS (base, layout) in explicit order
- Mount `<App />` inside `<StrictMode>`

## Used by
- Referenced in `index.html` as the module entry
